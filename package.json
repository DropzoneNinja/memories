{
  "name": "memories",
  "version": "1.3.3",
  "private": true,
  "description": "Self-hosted Frame TV photo system — release version source of truth for api/web/tv",
  "scripts": {
    "version": "node scripts/sync-versions.mjs"
  }
}
